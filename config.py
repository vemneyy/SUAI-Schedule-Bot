# config.py

import logging

logging.basicConfig(level=logging.INFO)

BOT_TOKEN = "BOT_TOKEN"

DATABASE_CONFIG = {
    'user': 'USER',
    'password': 'PASSW0RD',
    'database': 'DATABASE',
    'host': 'HOST'
}
